# Allowed SSH signers for git commit verification
# Format: <email> <key-type> <public-key>
# After running install.sh, add your key with:
#   echo "username@localhost.tld $(cat ~/.ssh/id_ed25519.pub)" >> ~/.ssh/allowed_signers
